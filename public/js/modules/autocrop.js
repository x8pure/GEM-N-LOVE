/* ================= PRODUCT IMAGE STABILIZER ================= */
/**
 * Preserves native crisp image assets without runtime canvas manipulation or scale flicker.
 */
export function normalizeTransparentImage(img) {
  // No-op to preserve crisp native asset dimensions and avoid runtime layout shift
}

export function initAutoCropNormalizer() {
  // Stabilized static rendering without dynamic canvas image rewriting
}
